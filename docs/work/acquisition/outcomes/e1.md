---
kind: outcome
status: partial
stage: E1-phase-0
baseline: 33a2a96
branch: work/acquisition/01-episode-selection
updated: 2026-09-25
---

# E1 — faza 0: konfiguracja, kontrole i próby oceniających

## Status: PARTIAL

Kod konfiguracji drugiego konta i panelu przeszedł wszystkie bramki. Przygotowano fixture, zweryfikowano 11 pewnych kontroli i wykonano pełnowymiarowe próby A/B/arbitra na koncie 1. **Pełny gate fazy 0 pozostaje niespełniony:** arbiter nie przeszedł kontraktu odpowiedzi po trzech próbach, a konto 2 nie ma wpisanych danych runtime. Niezależne review `opus5` potwierdziło D2 i bramki, a po poprawkach zaakceptowało 38 testów offline, ochronę dowodów oraz rozliczenie uwag bez nowych blockerów. Orkiestrator ponownie uzyskał 5201 passed i 18 skipped oraz utrwalił fixture i kod w trzech commitach wymienionych poniżej; ten dokument stanowi czwarty commit.

## Obowiązujące rozstrzygnięcia

Brief `brief-astra-e1-phase0b.md` zastąpił dwa fragmenty [planu E1](../plans/e1-wybor-odcinka.md):

1. Konto 2 ma standard konta 1: sekret `Settings.palantir_fallback_token` i adres `UserSettings.palantir_fallback_enrollment_base_url`, oba dostępne w panelu. Fabryka mapuje je na istniejące `LlmConfig.fallback_api_key` i `fallback_origin`. Dawne pola `Settings.palantir_fallback_origin/api_key` nie istnieją.
2. Budżet pozostaje **32000**. Wyłącznie `max_tokens`/`max_output_tokens` powoduje połowienie paczki. Nieprzejściowy 4xx zatrzymuje próbę, bez spekulacji o odrzuceniu budżetu; retry przejściowe należy do `LlmService`.

To rozwiązuje wcześniejsze blokady katalogu i adaptacji. Historyczny `budget-contract-blocker.json` jest dowodem syntetycznego mapowania błędów, nie bieżącą blokadą ani wynikiem live. `scripts/tmp/e1_contract_check.py` jest historyczny, niekompatybilny z nowymi polami konfiguracji i nie należy go uruchamiać.

## Konto 2 — konfiguracja i panel

Właściciel wpisuje dane w **Ustawienia → Połączenia → Palantir Foundry · konto 2**:

- „Ustaw / zmień klucz”: maskowany sekret, zapisywany dopiero po Enter; alternatywnie zmienna `ANISHIFT_PALANTIR_FALLBACK_TOKEN` w env lub `.env`.
- „Adres”: origin HTTPS enrollmentu, zapisywany jako `palantir_fallback_enrollment_base_url` w preferencjach panelu (`config/settings.json`). Adres ma tę samą walidację co konto 1; nie jest zmienną `Settings`.
- „Usuń klucz”: istniejąca ścieżka usuwania sekretu. Konto 2 nie ma osobnego przycisku probe; istniejąca próba konta 1 otrzymuje również konfigurację fallbacku.

Oba produkcyjne miejsca wywołania `palantir_llm_config` przekazują parę konta 2: tłumaczenie w `application/runtime.py` i probe w `application/service.py`. Reset preferencji zachowuje oba adresy. Niepełna para zachowuje działanie jednego konta zgodnie z istniejącym adapterem. Sekret nie trafia do repr ani do identyfikatorów własnych modeli; adres konta 2 jest pomijany w repr preferencji. Po review przywrócono dotychczasowe `palantir_enrollment_base_url: str = ""` konta 1; to jedyna produkcyjna korekta tej iteracji.

Dodano **16 przypadków D2** w `tests/config/test_model_settings.py` oraz **1 test integracyjny rzeczywistej edycji panelowej**. Dowody obejmują roundtrip, oba wywołania fabryki, literalność tokena i precedencję env, niepełne/niepoprawne pary, walidację adresu, ochronę repr/błędów/logów oraz failover HTTP 429/500/503 z zachowaniem route/query/body i zmianą auth. Test katalogu nadal wymaga pełnej zgodności zbioru pól; testy layoutu nie zostały osłabione.

`account-presence.json`: konto 1 token/adres **true/true**, konto 2 token/adres **false/false**. Są to wyłącznie booleany z `Settings()` i `load_user_settings()`. Nie odczytywano bezpośrednio prywatnego `.env` ani plików tokenów. `corpus_labels.py` celowo korzysta tylko z konta 1 w live fazy 0, zgodnie z briefem; podłączenie konta 2 w skrypcie i wpis właściciela są potrzebne przed fazą 1.

## Fixture i pewne kontrole

Zachowane wyniki poprzedniej części fazy 0:

| Wejście AniList | ID | Głębokość | Żądania |
| --- | ---: | ---: | ---: |
| Slime S1 | 101280 | 3 | 3 |
| Solo Leveling | 151807 | 3 | 2 |
| Haibane Renmei | 387 | 3 | 1 |
| Slime OVA | 106509 | 3 | 4 |
| Slime Diaries | 116741 | 3 | 1 |

Łącznie 11 żądań, wszystkie 200. Nie było błędu złożoności, więc nie próbowano mniejszej głębokości. Pierwsze dwa uruchomienia naprawiały lokalną agregację pełnych węzłów i pustych granic; zapisane odpowiedzi pozwoliły wznowić bez powtarzania HTTP. OVA wykorzystuje cały limit 4/4. Historyczne review potwierdziło offline traversal pięciu korzeni. Slime obejmuje `101280, 108511, 116742, 156822, 161802, 182205, 182206`; bezpośrednie OVA dołącza `106509`, Diaries zachowuje odrębność i krawędź `PARENT`. To dane wejściowe, nie dowód przyszłej implementacji `episode_target`.

Zachowano 4 fixture ani.zip i 8 Torrentio zgodne z archiwalnymi sumami, 5 franczyz oraz manifest. `identity-231.json` ma 231 unikalnych ID, wymagane pola zgodne ze źródłem, bez `heuristic`. Ponowna kontrola offline potwierdziła 19 plików fixture/manifestów i wartości wszystkich zachowanych pól.

**11/11 pewnych kontroli zweryfikowanych**, seed **20260925**; wszystkie **12 `local_number_assumption` wykluczone**. Pula:

```text
slime-s4e10:39
slime-s4e23:1
slime-s4e23:10
slime-s4e23:12
slime-s4e23:17
slime-s4e23:38
ova-1:5
ova-4:1
ova-4:4
ova-4:6
ova-4:7
```

`controls.json` zawiera indywidualne dowody i SHA-256 źródeł: `tests/fixtures/search/anizip__182205.json`, `anizip__106509.json` i `workspace/.archive/acquisition/evidence/sources/anchors/sonarr-ova.json`. Weryfikowane są mapowanie numeru, tytuł i widoczna kotwica w nazwie. Pula zawiera wyłącznie pozytywne historyczne etykiety `match`; wynik 10/10 nie dowodzi sam w sobie rozróżniania negatywnych przypadków. To ograniczenie wymaga decyzji właściciela przed fazą 1, nie jest blockerem commitowym D2.

## Próby modeli

Każda próba: **100 najdłuższych rekordów roboczych poza kontrolami + 10 różnych losowych kontroli**, szacunek wyjścia **14082**, `max_output_tokens=32000`, `reasoning_variant=None`, `max_retries=2`. A/B otrzymują niezależnie przemieszane wejścia bez historycznych ocen, heurystyki i odpowiedzi drugiego modelu. Jeden limit dwóch workerów obejmuje wszystkie role; każda próba ma osobny `LlmService`.

| Rola / próba | Alias | Finish reason | Tokeny wejścia / wyjścia / razem | Kontrakt JSON | Kontrole | Czas [s] |
| --- | --- | --- | --- | --- | --- | ---: |
| A / 1 | `foundry/gpt-6-astra` | `completed` | 34644 / 5358 / 40002 | tak | 10/10 | 66.076 |
| B / 1 | `foundry-anthropic/claude-opus-5` | `end_turn` | 49027 / 18378 / 67405 | tak | 10/10 | 187.941 |
| Arbiter / 1 | `foundry-google/gemini-3.1-pro-preview` | `stop` | 35431 / 6785 / 52355 | nie | brak wyniku | 118.869 |
| Arbiter / 2 | ten sam | `stop` | 35548 / 6557 / 55662 | nie | brak wyniku | 194.039 |
| Arbiter / 3 | ten sam | `stop` | 35431 / 7004 / 51081 | nie | brak wyniku | 121.043 |

Każde wywołanie miało jedną obserwowaną próbę transportową, **0 HTTP 429 i 0 HTTP 5xx**. Liczniki pochodzą z obserwatora `LlmService`; nie opisują ukrytych zdarzeń upstream. Koszt dostawcy: brak danych. Usage zapisano dokładnie według odpowiedzi silnika; suma Google nie musi być sumą dwóch widocznych składowych, więc nie rekonstruowano brakujących kategorii.

A i B potwierdziły parę **100+10 / 32000**. Zgodność robocza wyniosła **73/100**, czyli 27 niezgód. Nie wykonano arbitrażu tych niezgód: pełnowymiarowa próba arbitra zakończyła się stanem `owner`. Odrzucenie oznacza niespełnienie parsera całego kontraktu (składnia, liczność, kształt, ID, etykieta lub powód), nie dowiedziony konkretny błąd składni JSON. **Historyczne** metryki nie zachowują szczegółowej kategorii odrzucenia, a odpowiedzi nie były utrwalane, więc przyczyny nie można obecnie ustalić ani dopisać wstecz. Nie było ucięcia ani nieprzejściowego 4xx; nie zmieniano budżetu ani paczki. Limit ponowień jakości został wyczerpany; kolejna próba wymaga decyzji właściciela. Po review nie wykonano żadnego live.

**Propozycja D3: globalnie 2 workery na początek.** Taki limit sprawdzono, czasy A/B wyniosły około 66/188 s, nie zaobserwowano 429/5xx. Przy identycznych czasach 100 paczek na każdy z A/B daje około 3,53 h łącznej pracy workerów, czyli teoretycznie 1,76 h przy dwóch workerach, przed arbitrażem, retry i narzutem. To ilustracja z pojedynczego pilota, nie prognoza całego korpusu. Limit pozostaje propozycją; brak zaliczonego arbitra i konta 2 blokuje pełne zatwierdzenie D3.

## Skrypt i granice dowodu

`scripts/tmp/corpus_labels.py` realizuje próbę fazy 0, kontrole, walidację kontraktu, ograniczoną pulę, jeden zapisujący wątek, deterministyczne podziały i replay JSONL. Etykiety i odpowiedzi żyją tylko w pamięci. Wznowienie pomija ukończone próby i odtwarza podziały, ale **nie odtwarza wcześniejszych etykiet ani pełnej zgodności A/B**. Skrypt nie jest gotowym zbieraczem/etykieciarzem korpusu fazy 1.

23 testy offline sprawdzają oba powody ucięcia również przy poprawnym JSON, ceil-half i stałe kontrole/budżet, zatrzymanie na minimum, trwałe 4xx bez adaptacji, limit powodu, błędy i duplikaty ID, ograniczone ponowienia jakości, trzeci model przy powtórnym błędzie kontroli, niezależność A/B, limit aktywnych workerów, brak payloadów w journal oraz wznowienie podziałów i zapisów poza kolejnością. Istniejące testy LLM i nowe D2 sprawdzają granicę retry/failover; live nie wywołał retry transportu.

### Poprawki offline po review

- `_quality_result` zapisuje tylko statyczne kategorie `syntax/count/shape/id/label/reason/unknown_contract`. `JSONDecodeError` jest obsługiwany przed `ValueError`; dowolny komunikat wyjątku nie trafia do metryk. Rozdzielenie etykiety i powodu nie zmienia akceptowanego kontraktu parsera.
- Summary oblicza zgodność wyłącznie dla kompletu A/B wszystkich rekordów roboczych w pamięci. Po replay z niepełnymi etykietami zachowuje wcześniejszy pełny summary bajtowo, a przy jego braku zapisuje `null` dla obu liczników — nigdy zero ani wynik podzbioru jako globalny.
- `main` sprawdza kontrakt journalu i istniejących artefaktów przed zapisami oraz odczytem konfiguracji runtime. Zmiana SHA skryptu zatrzymuje replay starych prób bez nadpisania manifestu, kontroli i wyników.
- 15 nowych przypadków offline obejmuje kategorie i prywatne sentinele, dwa rzeczywiste przebiegi `main` na syntetycznej granicy completion z zachowaniem **73/100**, niepełne etykiety z pełnym/częściowym/brakującym summary oraz odrzucenie zmienionego kontraktu na kopiach historycznych dowodów. Etykiety pozostają tylko w pamięci; nie testowano modeli ponownie.

Nie uruchamiać ponownie `--live` jako sposobu diagnozy arbitra. Aktualny skrypt ma inny SHA niż wersja live i celowo odmawia replay jej journalu. Nowa diagnoza wymaga decyzji właściciela i osobnego zestawu dowodów; dodane kategorie nie wyjaśniają historycznych odrzuceń.

## Bramki

Baseline przekazany przez parenta: **5184 passed, 18 skipped**, 2026-09-24. Końcowe sprawdzenie 2026-09-25:

| Kontrola | Wynik |
| --- | --- |
| `uv run ruff check anishift/ tests/` | PASS |
| `uv run ruff format --check anishift/ tests/` | PASS, 544 pliki |
| `uv run mypy anishift/ tests/` | PASS, 546 plików |
| `uv run mypy --platform linux anishift/ tests/` | PASS, 546 plików |
| `uv run pytest` — odbiór orkiestratora i bramki przed commitami fixture/kodu | **5201 passed, 18 skipped, 1 warning** w każdym z trzech przebiegów: 39.40 s, 35.77 s, 32.95 s |
| `uv run pytest` po korekcie repr | 5200 passed, 1 failed, 18 skipped, 1 warning, 34.47 s; pojedynczy timeout automation |
| Jedno powtórzenie `test_material_identity_survives_bundle_metadata_preparation_processing_and_ready[True]` | **1 passed, 5.71 s** |
| D2 + katalog + `test_palantir_auth/accounts/requests.py` | 156 passed, 8.92 s |
| `uv run pytest tests/cli/test_interactive_settings_models.py tests/cli/test_interactive_settings_layout.py` | 32 passed, 5.59 s |
| `uv run pytest -n 0 scripts/tmp/test_corpus_labels.py` | 38 passed, 2.75 s po poprawkach offline |
| `uv run python scripts/tmp/corpus_labels_evidence.py` | PASS: 19 fixture/manifestów, 231 rekordów, 45 artefaktów <500 KiB, 7 dowodów immutable zgodnych z wcześniejszym inwentarzem |
| `git diff --check` | PASS |

Wcześniejsze failures kompletności layoutu usunięto przez rzeczywiste dodanie edytora konta 2 i wpisu jego adresu w pokryciu połączeń. Przed korektami i w niezależnym review pełny pytest miał 5201 passed / 18 skipped. Po korekcie repr pojedynczy test automation przekroczył oczekiwanie na `succeeded`; zgodnie z regułą powtórzono go raz i przeszedł. Nie zmieniano kodu automation ani testu. Wynik pełnego przebiegu i powtórzenia podano osobno. Ostrzeżenie pochodzi z deprecjacji `_UnionGenericAlias` w `google.genai`. Standardowy Ruff/mypy obejmuje aplikację i `tests/`, nie ignorowane skrypty tymczasowe; dowodem poprawionej wersji skryptu są testy offline, nie nowe próby modeli.

## Dowody i przekazanie

Lokalne dowody leżą w `workspace/.archive/acquisition/evidence/e1/`:

- `controls.json`: kotwice, ID i seed;
- `account-presence.json`: wyłącznie obecność danych kont;
- `trial-manifest.json`, `model-trials.jsonl`: kontrakt i metryki prób;
- `model-summary.json`: stany i zgodność A/B;
- `model-trial-metrics.json`: czytelna projekcja metryk bez list ID;
- `phase0b-inventory.json`: ścieżki, rozmiary i SHA-256 artefaktów;
- `offline-review-verification.json`: osobne SHA wersji live i bieżącej offline, suma testów oraz potwierdzenie niezmienności siedmiu historycznych artefaktów;
- wcześniejsze `franchise-summary.json`, `fixture-verification.json`, `handoff-inventory.json` i odpowiedzi AniList zachowują dowody pierwszej części.

SHA-256 fixture: `44382b317b2a1bf8e17a46f316536a051292414d3191f674ab9c88bbcef410f0`.
SHA-256 skryptu użytego live: `fb447d0594ca59ddfef2a033ebd3b0d5bca197e2937662c91494881066f9bd35`.
SHA-256 skryptu po poprawkach offline: `0f1c49225723d3ce1afcde232fa84f62e52522d2517e1f58f4880f25f975e4ff`.
SHA-256 aktualnych testów offline: `d223c18dc5799c72d163a7d2529ae9ef72706de8eaaa54f04a67af8efd1528cb`.
SHA-256 promptu: `973010ec5050f425bc68804e587d5d3a7c9a706c6164d81e1ffbb620c75285c6`.
SHA-256 kontroli: `994384833ce15c301c89f24a0875d6decfce237a32c447d569cf34d229ee8ab4`.

Skrypty `scripts/tmp/corpus_labels.py`, `corpus_labels_prompt.md`, `test_corpus_labels.py`, `corpus_labels_evidence.py` są ignorowane przez Git i wymagają osobnego przekazania z sumami. Runtime prompty i odpowiedzi nie były zapisywane. Nie zmieniano `anishift/services/llm/`, planu/spec/UX/masterplanu; nie uruchamiano dalszych faz, korpusu, live Torrentio, mediów ani rezydenta.

### Commity po odbiorze

Pierwsze trzy commity wykonano po zielonych bramkach i z działającymi hookami. Dokument utrwala wynik jako czwarty commit o wskazanym tytule. Pliki dodawano jawnie; artefakty lokalne pozostają poza commitami.

1. `2cb7a27` — `test(services): record franchise, ani.zip and torrentio fixtures for e1`
   - `tests/fixtures/search/manifest.json`
   - `tests/fixtures/search/anilist__franchise__101280.json`
   - `tests/fixtures/search/anilist__franchise__106509.json`
   - `tests/fixtures/search/anilist__franchise__116741.json`
   - `tests/fixtures/search/anilist__franchise__151807.json`
   - `tests/fixtures/search/anilist__franchise__387.json`
   - `tests/fixtures/search/anizip__101280.json`
   - `tests/fixtures/search/anizip__106509.json`
   - `tests/fixtures/search/anizip__182205.json`
   - `tests/fixtures/search/anizip__387.json`
   - `tests/fixtures/search/torrentio__kitsu-1-1.json`
   - `tests/fixtures/search/torrentio__kitsu-354-1.json`
   - `tests/fixtures/search/torrentio__kitsu-41024-4.json`
   - `tests/fixtures/search/torrentio__kitsu-42022-1.json`
   - `tests/fixtures/search/torrentio__kitsu-42022-4.json`
   - `tests/fixtures/search/torrentio__kitsu-49235-10.json`
   - `tests/fixtures/search/torrentio__kitsu-49235-23.json`
   - `tests/fixtures/search/torrentio__kitsu-49235-24.json`
2. `3a3c685` — `test(application): add labelled identity fixture for e1`
   - `tests/fixtures/acquisition/identity-231.json`
3. `1b9a49e` — `feat(config): add a second palantir account next to the first`
   - `anishift/config/settings.py`
   - `anishift/config/user_settings.py`
   - `anishift/config/field_catalog.py`
   - `anishift/application/runtime.py`
   - `anishift/application/service.py`
   - `anishift/cli/interactive/settings.py`
   - `tests/config/test_model_settings.py`
   - `tests/config/test_field_catalog.py`
   - `tests/cli/test_interactive_settings_models.py`
4. `docs(repo): record e1 phase 0 baseline and evaluator trial`
   - `docs/work/acquisition/outcomes/e1.md`

## Następny punkt decyzji

Review `opus5` oraz weryfikację poprawek zakończono bez otwartych błędów implementacji. Przed fazą 1 potrzebne są wpis właściciela i podłączenie konta 2 w skrypcie, decyzja o ograniczeniu wyłącznie pozytywnych kontroli oraz decyzja o kolejnej próbie arbitra po wyczerpanym limicie jakości. Bez zaliczonej pełnej odpowiedzi arbitra i dostępnych dwóch kont nie przechodzić do fazy 1.
