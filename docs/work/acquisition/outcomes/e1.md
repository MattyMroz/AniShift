---
kind: outcome
status: partial
stage: E1-phase-1-and-N-03
baseline: ddd8179
branch: work/acquisition/01-episode-selection
updated: 2026-09-25
---

# E1 — bieżący stan korpusu i otwarte decyzje

## Status: PARTIAL — zbieranie i ocena działają

**Pomiar: 2026-09-25, 18:12:08–18:12:33 +02:00.** Zbieracz i ocena mają
`writer_active=true`, `liveness=writer_lock_held`; ocena zwraca `running`
i `source_active`, zbieracz `active_stops=[]`, `complete=false`.
`main_complete=true` oznacza ukończenie zbierania części głównej, a nie jej oceny.
Trwa zbieranie rezerwy i ocena głównego korpusu; bramka E1 pozostaje otwarta.

Źródłem są `powershell.exe -NoProfile -File scripts/tmp/acquisition_collect_local.ps1 -Action status`
i `powershell.exe -NoProfile -File scripts/tmp/corpus_labels_local.ps1 -Action status`
oraz bezpieczne agregacje zakończonych linii journalów. Odczyty działających procesów nie są atomowe;
poniżej podano czas każdego zestawu, zamiast wyrównywać różne liczniki.

| Korpus — status zbieracza o 18:12:32 | Tytuły | Rekordy | Rozliczone (`evaluated`) |
| --- | ---: | ---: | ---: |
| `main` | 2050 | 51 909 | 12 582 |
| `reserve` | 112 | 1201 | 0 |

`main` obejmuje pierwotne 2000 tytułów i 50 dodatkowych długich serii;
`long_shortage=0`. Próby źródeł: AniList **5877**, ani.zip **2162**,
Torrentio **3746**. `reserve` pozostaje osobną pulą poza pierwszym egzaminem.

| Ocena — status o 18:12:10 | Wartość |
| --- | ---: |
| Rekordy z przyjętą oceną A / B | 22 000 / 13 100 |
| `settled` / `complete` / `owner` | 12 548 / 12 484 / 64 |
| Pula przyjęta przez ocenę (`available`) | 51 815 |
| Limity workerów A / B | 26 / 12 |
| Zapas A bez B / limit dopuszczania nowych A | 8900 / 2000 |
| Historyczne pomyłki kontroli A / B | 0 / 8 |
| Wyłączone kontrole / wszystkie kontrole | 2 / 24 |

Limity workerów nie są liczbą aktywnych wywołań. Historyczna nadwyżka A blokuje
nowe A do zejścia do dopuszczalnego zapasu; rezerwacje A w toku też zajmują limit,
a niezakończone B nie zwalniają miejsca. Modele oceny i dyskusji to Astra/Opus 5.
A używa 100 rekordów roboczych + 10 kontroli, B 50 + 10, budżet 32 000,
timeout 900 s. Konto 2 jest opcjonalne: status potwierdza adres, ale brak tokena.

### Etykiety i znaczenie liczników

Agregacja `%USERPROFILE%/acquisition-labels/labels.jsonl` o **18:12:32.192699 +02:00**:

| Etykieta zdarzenia `verdict` | Liczba zdarzeń |
| --- | ---: |
| `poprawny` | 11 379 |
| `błędny` | 1138 |
| `nie da się ustalić` | 64 |
| **Razem** | **12 581** |

To rozkład **zdarzeń `verdict`**, nie suma niezależnych odpowiedzi A/B.
W tym odczycie było również **12 581 unikalnych ID**, więc nie wystąpiły duplikaty
werdyktów. Stany tych zdarzeń: `complete=12 517`, `owner=64`.
`settled` liczy unikalne rekordy z werdyktem, także skierowane do właściciela;
nie oznacza liczby poprawnych ani ostatecznie zaakceptowanych etykiet.
`owner` jest licznikiem spraw do rozstrzygnięcia, bez dodatkowej próbki 50 zgodnych.
Różnice względem statusu o 18:12:10 i `evaluated` zbieracza wynikają z kolejnych
momentów odczytu. `available` opisuje przyjęty snapshot wejścia, nie cały korpus.

### Błędy transportu i kontrole

- **Zbieracz, journal o 18:12:08:** 3 zdarzenia odpowiedzi HTTP 429,
  0 HTTP 5xx, 0 timeoutów i 0 błędów transportu; status grupuje tylko 2 wpisy
  `error_code=http_429`, więc ten agregat błędów nie zastępuje licznika odpowiedzi.
  Historyczne blokady pozostają w journalu, ale `active_stops=[]`.
- **Ocena:** 0 HTTP 429 i 0 HTTP 5xx w zdarzeniach `batch`
  (agregacja o 18:12:32); potwierdzone timeouty A/B: 0/1 według statusu.
  Agregacja HTTP nie obejmuje zagnieżdżonych metryk dyskusji w `verdict`.
  Są to zapisane obserwacje, bez domniemywania ukrytych prób upstream.
- **Kontrole:** B ma łącznie 8 pomyłek w historii prób: dwie kontrole po 3
  i dwie po 1; A ma 0. Dwie kontrole są wyłączone i wymagają audytu właściciela.
  Dowód: `workspace/.archive/acquisition/evidence/e1/corpus/control-audit.json`.

## Poprawki lokalne z 2026-09-25

- `scripts/tmp/acquisition_probe.py:Probe._attempt`, `Probe.fetch` i `Probe._wait_for_cooldown`: HTTP 429 **każdego źródła** prowadzi do cooldownu i ponowienia, a nie trwałej blokady, również gdy odpowiedź zawiera markery challenge.
- `scripts/tmp/corpus_labels.py:published_events`, `published_rows`, `project_title` i `_verified_source`: ocena czyta tylko ukończone publikacje z kontrolą SHA, metadane źródeł tylko z odpowiedzi 200/`received`, pomija niepoprawny JSON/Unicode tych metadanych, a `safe_failure` wskazuje nazwę funkcji na ścieżce błędu JSON bez wypisywania zawartości ani pełnej ścieżki pliku.
- `scripts/tmp/corpus_labels.py:Evaluator.__call__`, `_context_exceeded` i `successors`: HTTP 413 lub za długi kontekst dzieli paczkę, a niedzielny pojedynczy rekord kieruje do właściciela.
- `scripts/tmp/corpus_labels.py:AdaptiveLimit.observe`, `ModelLimits`, `a_backlog` i `run_batches`: skalowanie korzysta z sygnałów przepustowości, 429/5xx/timeouty blokują wzrost, błędy jakości nadal blokują przyjęcie oceny, zapas A bez B ma limit 2000 z blokadą nowych A przy historycznej nadwyżce, a B najwyżej 12 workerów.
- `scripts/tmp/corpus_labels.py:Checkpoints.__init__`, `_script_only_change` i `authorize_evaluator_retries`: jawna autoryzacja dopuszcza ograniczoną zmianę polityki wykonania z zachowaniem tożsamości danych, promptów i modeli oraz osobne jednorazowe ponowienie dwóch istniejących paczek B z HTTP 400.

### Dowody autoryzacji i odzyskania

Bezpieczna projekcja `%USERPROFILE%/acquisition-labels/labels.jsonl`:

| Zdarzenie | Czas +02:00 | Dowód administracyjny |
| --- | --- | --- |
| Aktualizacja polityki, linia 11 357 | 16:14:01.872386 | `contract_updated`, `owner_script_change`, `identity_bootstrap=true`; przypięte `execution_policy.*`, semantyka etykiet, fingerprint parsera i tożsamość archiwów |
| Ponowienie pierwszej B/400, linia 11 359 | 17:20:49.393443 | `evaluator_retry_authorized`, `owner_cli_retry_evaluator_blocked`; jeden późniejszy zapis tej samej próby, `complete` |
| Ponowienie drugiej B/400, linia 11 360 | 17:20:50.095099 | Ta sama autoryzacja; jeden późniejszy zapis tej samej próby, `complete` |
| Najnowsza aktualizacja, linia 11 363 | 17:37:11.538536 | `contract_updated`, `owner_script_change`, `identity_bootstrap=false`, jedyne zmienione pole: `scripts.corpus_labels.py` |

Najnowszy SHA skryptu oceny:
`015d995513d8d1e7e2abd07bdf826527d5823e4c12646ada36790e1809c0334f`
(poprzedni: `bb24c06e99ddea50ee56fa62fdbe07caf8dc50ac8c42d9546a2142c9b5dc5500`).
Jest zgodny z wartością statusu. SHA zbieracza ze statusu:
`42174805a35f0fdb6805d0120e2deaf051b1f8adc8aa4efe6c9d95bdd2b196e8`.
Sumy prefiksów journalu obu wskazanych aktualizacji zgadzają się z ich
`previous_journal_sha256`; zachowano wcześniejsze bajty historii.
Autoryzacje retry dotyczą zapisanych blokad, nie przyszłych błędów.
Istnieje historyczne HTTP 413 / `LLM_CONTEXT_EXCEEDED`; podział po takim błędzie
jest obecny w kodzie, lecz odczyt o 18:11:47 wykazał **0 zdarzeń podziału
`context_exceeded`**. Brak zapisanego podziału po tym błędzie oznacza, że
skuteczność tej ścieżki live pozostaje niepotwierdzona.

## N-03 — PARTIAL, decyzja przed E2

Zachowany wynik pomiaru z 2026-09-25: metadane i jednoznaczne mapowanie pliku
**3/3**, zapisane bajty treści **0**, ale ustawienie priorytetów utworzyło
po jednym pustym `.mkv` na kandydata. Zerowy rozmiar nie oznacza braku plików;
właściciel musi zaakceptować ten efekt w stagingu przed E2.
Dowód: `workspace/.archive/acquisition/evidence/e1/magnet/probe-20260925T103445Z.json`.
To zachowany pomiar, nie nowa próba tej sesji.

## Czeka na właściciela

1. **Przegląd etykiet:** rozstrzygnąć listę spraw `owner` (obecnie 64:
   spory po dyskusji, zgodne nieustalone lub odrzucone oceny według kontraktu)
   oraz sprawdzić **50 losowych zgodnych**, seed `20260925`, z materiałów
   `workspace/.archive/acquisition/evidence/e1/corpus/owner-review*.json`;
   bieżący licznik nie dowodzi ukończenia audytu.
2. **Audyt kontroli:** rozstrzygnąć dwie wyłączone kontrole na podstawie
   `control-audit.json` przed uznaniem jakości etykiet za zamkniętą.
3. **Konto 2:** opcjonalnie uzupełnić token; jego brak nie zatrzymuje obecnej oceny.
4. **N-01:** rozliczyć pomiar i decyzję źródeł według [planu, §9 N-01](../plans/e1-wybor-odcinka.md#n-01--opóźnienie-pojawienia-się-odcinka):
   7 dni lokalnie na Windows, start w fazie 5 po fazach 3–4; przy mniej niż
   5 pełnych porównywalnych obserwacjach decyzja właściciela przed E2/E3,
   bez automatycznego przedłużenia; opóźnienie Torrentio ponad Nyaa +1 h
   lub brak wydania w pełnym horyzoncie, gdy Nyaa je ma, wymaga RSS Nyaa w E3,
   a lepsze wydanie dostępne tylko w Nyaa — łączenia
   źródeł w E2; obecny zbieracz korpusu nie jest pomiarem N-01.
5. **Podział przed fazą 2:** ustalić z opiekunem liczebności/proporcje części
   roboczej i odłożonej z rzeczywistych warstw `main`, przed losowaniem zapisać
   algorytm, seed i SHA w `split-manifest.json`, grupować franczyzy oraz
   współdzielone hashe/pliki i potwierdzić odmowę odczytu części odłożonej
   przez autora; `reserve` pozostaje poza pierwszym egzaminem, a materiały
   już widziane przez autora i ich grupy mogą być tylko robocze
   ([plan, §9 N-02 i faza 2](../plans/e1-wybor-odcinka.md#n-02--budowa-heurystyki-od-zera-i-jednorazowy-egzamin)).
6. **N-03:** zaakceptować albo odrzucić puste pliki stagingu przed E2.

## Zakres i weryfikacja tej aktualizacji

Baseline: `work/acquisition/01-episode-selection @ ddd8179`, czyste drzewo
na wejściu. Zmieniono wyłącznie ten raport; wykonano odczyty statusów,
agregacje metadanych i kontrolę odnośników do implementacji. Procesy pozostawiono
działające. Skrypty `scripts/tmp/` i lokalne dowody są ignorowane przez Git
i nie są dostarczane samym checkoutem.

Kontrola dokumentu: `git diff --check` — PASS. W bieżącej sesji wykonano
następujące bramki dla aktualizacji dokumentacji:

| Bramka | Wynik |
| --- | --- |
| `uv run ruff check anishift/ tests/` | PASS |
| `uv run ruff format --check anishift/ tests/` | PASS, 544 pliki |
| `uv run mypy anishift/ tests/` | PASS, 546 plików |
| `uv run pytest` | 5201 passed, 18 skipped, 1 warning (deprecjacja `google.genai`), 38,68 s |

To bieżące bramki repozytorium, nie dowód działania live ignorowanych skryptów.
Niezależny przegląd read-only `opus5`: **PASS**; weryfikacja końcowych
doprecyzowań następuje przed commitem. Zakończenie oceny, audyt właściciela,
podział, egzamin i decyzje N-01/N-03 pozostają otwarte.
