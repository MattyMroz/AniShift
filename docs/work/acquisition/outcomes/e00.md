---
kind: outcome
status: kod zaakceptowany (runda 6: trzy niezależne PASS bez uwag); checkpoint tłumaczenia właściciela oczekuje
baseline: c32c794
completed: 2026-09-23
---

# Outcome E00: modele LLM AniShift 1:1 z OpenCode

## Faza 0 — konto 1

Próby na żywo wykonane 2026-09-23 surowymi żądaniami `httpx` skryptem `scripts/tmp/e00_smoke.py` na koncie 1 (token z `Settings().palantir_token`, adres z `load_user_settings()`; do trasy brany tylko origin). Lista modeli, opcji i wariantów pochodzi z `agents/skills/opencode/assets/opencode.jsonc`; z `baseURL` brana jest wyłącznie ścieżka. Wiersz na każdą próbę: `workspace/.archive/acquisition/evidence/e00/smoke.json` (bez hostów, tokenów, nagłówków i ciał). Pliki testowe obok: `red.png` (64×64), `anishift7.pdf`, `tone.wav` (1 s, 440 Hz), `blue.mp4` (1 s, niebieski + ton 440 Hz).

Konto 2 nie było sprawdzane: w `.env` nie ma jeszcze `ANISHIFT_PALANTIR_TOKEN_2`. Skrypt przyjmuje `--account2-origin <origin>` (opcjonalnie `--skip-account1`) i wtedy wysyła tekst na pierwszym modelu każdej rodziny.

### Liczby

| Rodzina | Przypadki macierzy (ok / nie) | Zapisane próby (ok / nie) |
| --- | --- | --- |
| OpenAI (5 modeli) | 68 (65 / 3) | 72 (65 / 7) |
| Anthropic (7) | 53 (50 / 3) | 63 (50 / 13) |
| Google (6) | 61 (61 / 0) | 63 (61 / 2) |
| xAI (2) | 16 (16 / 0) | 16 (16 / 0) |
| **Razem** | **198 (192 / 6)** | **214 (192 / 22)** |

Próba = jedna forma żądania; przypadek przechodzi, gdy przejdzie którakolwiek forma. Dodatkowe próby to formy zapasowe po odmowie lub złej odpowiedzi. Nie wystąpił żaden 429, 401/403 ani 5xx. Część przypadków powtórzono po poprawce czytelności komunikatów błędów i kryterium „odpowiedź ucięta limitem”; `smoke.json` trzyma ostatni wynik każdej próby.

Z 6 nieprzechodzących przypadków 3 to oczekiwane odrzucenie temperatury w gałęzi „nie wysyła” (niżej), a 3 to warianty z OpenCode, których nie obsługuje sam model.

### Wyniki per model

`ok` = HTTP 200 i poprawna odpowiedź (obraz: „czerwony”, PDF: `ANISHIFT7`, wideo: „niebieski”, audio: dowolne słowo, bez ucięcia limitem). Warianty: przeszły / zadeklarowane w OpenCode.

| Model | Tekst | Obraz | PDF | Audio | Wideo | Warianty | Chat Completions | Strumień Responses | Rola system / developer |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `foundry/gpt-6-astra` | ok | ok | ok | — | — | 5/5 | ok | ok | ok / ok |
| `foundry/gpt-5.6-sol` | ok | ok | ok | — | — | 6/6 | ok | ok | ok / ok |
| `foundry/gpt-5.6-terra` | ok | 200, ok przy 256 px ¹ | ok | — | — | 6/6 | ok | ok | ok / ok |
| `foundry/gpt-5.6-luna` | ok | 200, ok przy 256 px ¹ | ok | — | — | 6/6 | ok | ok | ok / ok |
| `foundry/gpt-5.5` | ok | ok | ok | — | — | 5/6 (`max`: 400) | ok | ok | ok / ok |
| `foundry-anthropic/claude-opus-5` | ok | ok | ok | — | — | 5/5 | — | — | — |
| `foundry-anthropic/claude-opus-4-8` | ok | ok | ok | — | — | 5/5 | — | — | — |
| `foundry-anthropic/claude-opus-4-7` | ok | ok | ok | — | — | 5/5 | — | — | — |
| `foundry-anthropic/claude-opus-4-6` | ok | ok | ok | — | — | 4/5 (`xhigh`: 400) | — | — | — |
| `foundry-anthropic/claude-opus-4-5` | ok | ok | ok | — | — | brak wariantów | — | — | — |
| `foundry-anthropic/claude-sonnet-5` | ok | ok | ok | — | — | 5/5 | — | — | — |
| `foundry-anthropic/claude-sonnet-4-6` | ok | ok | ok | — | — | 4/5 (`xhigh`: 400) | — | — | — |
| `foundry-google/gemini-3.1-pro-preview` | ok | ok | ok | 200, ok przy większym limicie ² | ok | 5/5 | — | — | — |
| `foundry-google/gemini-3.8-flash` | ok | ok | ok | ok | ok | 5/5 | — | — | — |
| `foundry-google/gemini-3.7-flash` | ok | ok | ok | ok | ok | 5/5 | — | — | — |
| `foundry-google/gemini-3.6-flash` | ok | ok | ok | ok | ok | 5/5 | — | — | — |
| `foundry-google/gemini-3.5-flash` | ok | ok | 200, ok przy większym limicie ² | ok | ok | 5/5 | — | — | — |
| `foundry-google/gemini-3.5-flash-lite` | ok | ok | ok | ok | ok | 5/5 | — | — | — |
| `foundry-xai/grok-4.6` | ok | ok | 400 (oczekiwane) | — | — | 2/2 | — | — | ok / ok |
| `foundry-xai/grok-4.5` | ok | ok | 400 (oczekiwane) | — | — | 2/2 | — | — | ok / ok |

¹ Proxy przyjmuje formę z planu (200). GPT-5.6 Terra i Luna źle odczytały jednolity obraz 64×64 („Nieznany”, „Czarny”; także z `detail: "high"`); ten sam obraz 256×256 w tej samej formie dał „Czerwony”. GPT-5.5 w pierwszym przebiegu też odpowiedział „Nieznany”, w powtórce „Czerwony”. To jakość odczytu bardzo małego obrazu, nie odmowa formy.

² Forma przyjęta (200), ale odpowiedź ucięta (`MAX_TOKENS`) przy `maxOutputTokens` 256 i domyślnym `thinkingBudget: -1`; z limitem 2304 poprawna. W pierwszym przebiegu to samo zdarzyło się Gemini 3.6 i 3.5 (audio) oraz 3.6 (PDF), w powtórce przeszły z limitem 256. Wniosek: przy budżecie dynamicznym limit 256 bywa za mały; dotyczy tylko prób, bo AniShift wysyła 32000.

Czasy: najdłuższa próba 21,2 s; suma czasów zapisanych prób 551 s.

### Potwierdzone formy żądań

| Protokół | Forma | Wynik |
| --- | --- | --- |
| OpenAI `/responses` | `{"model", "input": [{"role":"user","content":[{"type":"input_text",...}, pliki]}], "max_output_tokens", "store": false}` | 200 na 5/5 modeli |
| OpenAI `/responses` — obraz | `{"type":"input_image","image_url":"data:image/png;base64,..."}` (bez `detail`) | 200 na 5/5 |
| OpenAI `/responses` — PDF | `{"type":"input_file","filename":"document.pdf","file_data":"data:application/pdf;base64,..."}` | 200 i `ANISHIFT7` na 5/5 |
| OpenAI / xAI — wariant | `"reasoning": {"effort": "<wariant>"}` | 200 dla każdego wariantu obsługiwanego przez model |
| OpenAI / xAI — instrukcja | wiadomość `{"role":"system"|"developer","content":"<string>"}` przed `user` | obie role 200 na 7/7 modeli |
| OpenAI `/chat/completions` | dzisiejsza forma AniShift (`max_completion_tokens`) | 200 na 5/5 |
| xAI `/responses` | jak OpenAI, `store: false` | 200 na 2/2; obraz 200 |
| xAI `/responses` — PDF | `input_file` | 400 `unsupportedType=input_file` (zgodnie z `foundry-proxy.md`) |
| Anthropic `/messages` | nagłówek `anthropic-version: 2023-06-01`; `"thinking": {"type":"adaptive","display":"summarized"}` | 200 na 6/6 modeli z `thinking`; `display` przyjmowane |
| Anthropic — wariant | `"output_config": {"effort": "<wariant>"}` obok `thinking`, bez nagłówka `anthropic-beta` | 200 dla każdego poziomu obsługiwanego przez model |
| Anthropic — obraz / PDF | `{"type":"image","source":{"type":"base64",...}}` / `{"type":"document","source":{"type":"base64","media_type":"application/pdf",...}}` | 200 i poprawna odpowiedź na 7/7 |
| Google `generateContent` | `generationConfig.thinkingConfig` = `{"includeThoughts", "thinkingBudget"}`, `maxOutputTokens` = budżet + 256 | 200 dla wszystkich 5 wariantów na 6/6 modeli, także budżet 24576 |
| Google — obraz / PDF / audio / wideo | `{"inlineData":{"mimeType":...,"data":...}}` przed częścią tekstową | 200 na 6/6 dla każdej modalności |

Obserwacje z odpowiedzi:

- Google bez strumienia przy `includeThoughts: true` zwraca część z `thought: true` w każdej odpowiedzi (`thought_parts: 1` w `smoke.json`) — potwierdza potrzebę odfiltrowania `thought` w `normalize` (zakres planu, pkt 6).
- Anthropic z `display: "summarized"` zwraca bloki `thinking` przed `text`, gdy model myśli; bywa też sam `text` (adaptive).
- OpenAI i xAI w `output` często mają element `reasoning` przed `message` (także przy `store: false`); tekst jest tylko w `message` → `output_text`.

### Strumień OpenAI `/responses`

- `stream: true` działa na 5/5 modeli. Kolejność typów zdarzeń: `response.created`, `response.in_progress`, `response.output_item.added`, `response.content_part.added`, `response.output_text.delta`, `response.output_text.done`, `response.content_part.done`, `response.output_item.done`, `response.completed`. Przy GPT-5.5 przed częścią tekstową jest dodatkowa para `output_item.added`/`output_item.done` elementu `reasoning`. Proxy nie wycina zdarzeń OpenAI (inaczej niż xAI).
- `response.output_text.delta` przychodzi; zdarzenie końcowe `response.completed` niesie pełny obiekt `response`.
- Ucięcie (`gpt-5.6-luna`, `max_output_tokens: 16`, długie polecenie): zdarzenia `response.created`, `response.in_progress`, `response.output_item.added`, `response.output_item.done`, `response.incomplete`; w `response.incomplete` `status: "incomplete"`, `incomplete_details.reason: "max_output_tokens"`, a `output` ma tylko element `reasoning` (zero tekstu).
- Zdarzenia `error` ani `response.failed` nie wystąpiły; ich kształt nie jest potwierdzony na żywo.

### Temperatura (0.3)

| Przypadek | Model | Polityka planu | Wynik proxy | Rozstrzygnięcie |
| --- | --- | --- | --- | --- |
| OpenAI domyślny | `gpt-5.6-luna` | nie wysyła | 400 „Unsupported parameter: 'temperature' is not supported with this model.” | polityka zgodna; wysłanie byłoby błędem |
| OpenAI `high` | `gpt-5.6-luna` | nie wysyła | 400 (jw.) | polityka zgodna; wysłanie byłoby błędem |
| OpenAI `none` | `gpt-5.6-luna` | wysyła | 200 | bez zmian |
| xAI domyślny | `grok-4.6` | nie wysyła | 200 | przyjęta, pomijana z polityki |
| xAI `low` | `grok-4.6` | nie wysyła | 200 | przyjęta, pomijana z polityki |
| Claude z `thinking` | `claude-opus-5` | nie wysyła | 400 „`temperature` may only be set to 1 when thinking is enabled or in adaptive mode.” | polityka zgodna; wysłanie byłoby błędem |
| Claude bez `thinking` | `claude-opus-4-5` | wysyła | 200 | bez zmian |
| Gemini | `gemini-3.5-flash-lite` | wysyła | 200 | bez zmian |

Żadna gałąź „wysyła” nie została odrzucona, więc polityka temperatury z planu zostaje bez zmian. Te trzy 400 są w `smoke.json` jako `ok: false` (kryterium próby: „proxy przyjęło”), ale potwierdzają politykę.

### Rozstrzygnięcia reguł decyzji fazy 0

| Reguła z planu | Wynik |
| --- | --- |
| Tekst przez `/responses` nie działa dla któregoś modelu OpenAI → stop (D2) | **Działa na 5/5.** Brak BLOCKERA; D2 bez zmian. |
| Strumień `/responses` nie działa → OpenAI bez strumienia | **Strumień działa** (`output_text.delta` + `response.completed`; ucięcie daje `response.incomplete` z `max_output_tokens`). `openai_responses` idzie ze strumieniem; pasek postępu jak dziś. |
| `effort` Anthropic nie przechodzi jako `output_config.effort` → forma z ruchu OpenCode | **Przechodzi** jako `output_config.effort`, bez nagłówka beta. Pole `effort` na najwyższym poziomie proxy odrzuca (`unrecognizedProperty=effort`). |
| `thinking.display` Anthropic „według fazy 0” | **Przyjmowane**: `thinking: {"type":"adaptive","display":"summarized"}` przechodzi na wszystkich 6 modelach z `thinking`; można je wysyłać 1:1. |
| Formy plików (tabela planu) | **Potwierdzone** dla wszystkich rodzin i modalności; xAI PDF → 400 `unsupportedType=input_file`, jak przewidziano. |
| Temperatura | Polityka bez zmian (tabela wyżej). |
| Rola instrukcji Responses | `system` i `developer` przechodzą na 7/7 modeli OpenAI i xAI → zostaje `system`, jak w planie. |
| Wariant zadeklarowany w OpenCode nie przechodzi → podejrzenie ruchu OpenCode; bez działającej formy stop | **3 warianty nie przechodzą w żadnej formie** — odmawia sam model, nie schemat proxy (niżej). Do decyzji parenta. |

### Odchylenia od planu

Tabela form żądań w planie: **brak odchyleń kształtu pól** — każda forma z planu przeszła bez zmian.

Odchylenia od założeń planu (nie od form):

| Pole / miejsce | Plan / OpenCode | Stan faktyczny |
| --- | --- | --- |
| Wariant `xhigh` modeli `claude-opus-4-6` i `claude-sonnet-4-6` (`output_config.effort: "xhigh"`) | OpenCode deklaruje `low`…`max` z `xhigh` | 400 od modelu: „This model does not support effort level 'xhigh'. Supported levels: high, low, max, medium.” Tak samo z `anthropic-beta: effort-2025-11-24` i bez `display`; `effort` na najwyższym poziomie → 400 `unrecognizedProperty=effort`. |
| Wariant `max` modelu `gpt-5.5` (`reasoning.effort: "max"`) | OpenCode deklaruje `none`…`max` | 400 od modelu: „Unsupported value: 'max' is not supported with the 'gpt-5.5-2026-04-24' model. Supported values are: 'none', 'low', 'medium', 'high', and 'xhigh'.” |
| Faza 0 zakładała `max_output_tokens` 64 „gdzie się da” | — | Przy żądaniach z rozumowaniem (OpenAI/xAI poza `none`, Claude z `thinking`) użyto limitu 2048, bo tokeny rozumowania liczą się do limitu i 64 dawałoby puste odpowiedzi (por. ucięcie przy 16). Próby bez rozumowania: 64. |

Te trzy warianty to błąd konfiguracji OpenCode (asset skilla), nie formy AniShift: OpenCode wysyła `effort` przez AI SDK w tym samym polu, więc najpewniej te warianty zawodzą też w OpenCode. Nie sprawdzono tego podejrzeniem ruchu OpenCode (poza zakresem tej fazy). Skutek dla E00 przy odczycie 1:1 (D1): AniShift pokaże te warianty, a wybranie ich skończy się fatalnym 400 (`LlmRequestError`).

### Niepowodzenia (pełna lista)

| Model | Test | Formy | Status | Komunikat (skrócony) |
| --- | --- | --- | --- | --- |
| `claude-opus-4-6` | `variant:xhigh` | 6 form | 400 | effort level 'xhigh' not supported; top-level `effort` → `unrecognizedProperty=effort` |
| `claude-sonnet-4-6` | `variant:xhigh` | 6 form | 400 | jw. |
| `gpt-5.5` | `variant:max` | plan | 400 | 'max' not supported; supported: none, low, medium, high, xhigh |
| `gpt-5.6-luna` | `temperature:openai-default` | plan | 400 | 'temperature' is not supported with this model (oczekiwane) |
| `gpt-5.6-luna` | `temperature:openai-high` | plan | 400 | jw. (oczekiwane) |
| `claude-opus-5` | `temperature:claude-thinking` | plan | 400 | temperature only 1 with thinking/adaptive (oczekiwane) |
| `gpt-5.6-terra`, `gpt-5.6-luna` | `modality:image` | plan, `detail: high` | 200 | zła odpowiedź na obrazie 64 px; 256 px ok |
| `gemini-3.1-pro-preview` (audio), `gemini-3.5-flash` (PDF) | `modality:*` | plan | 200 | `MAX_TOKENS` przy limicie 256; z limitem 2304 ok |

### Niepewności

- Kształt zdarzeń `error` i `response.failed` w strumieniu Responses oraz odpowiedzi JSON ze `status: "failed"` nie wystąpił; mapowanie z planu pozostaje oparte na dokumentacji.
- Konto 2 niesprawdzone (brak tokena 2).
- Odczyt jednolitego obrazu 64×64 przez GPT-5.6 Terra/Luna (i raz GPT-5.5) jest niestabilny; forma jest potwierdzona, jakość dla małych obrazów nie.

## Faza 6 — próby przez finalny `LlmService`

Próby na żywo wykonane 2026-09-24 na koncie 1, na kodzie `54d7a3b` (gałąź `work/acquisition/00-model-catalog-v2`), skryptem `scripts/tmp/e00_live.py` (poza commitami): każde żądanie przechodzi przez `palantir_llm_config` → `LlmService`, tak jak w aplikacji. Wynik wiersz po wierszu: `workspace/.archive/acquisition/evidence/e00/live-e00.json` (bez tokenów, adresów i treści żądań). Pliki próbne jak w fazie 0.

Kod tej ścieżki różni się od sprawdzanego kodu wyłącznie przemianowaniami bez zmiany zachowania z Rozstrzygnięcia 19 (`_first_candidate`, lokalny `model`).

| Przypadek | Liczba | Wynik |
| --- | --- | --- |
| Tekst z opcjami domyślnymi | 22 modele | 22 / 22 |
| Obraz (`red.png`) | 22 | 22 / 22 technicznie; 21 × „czerwony”, 1 × „czarny” (`gpt-5.6-luna`, znana słabość małego obrazu) |
| PDF (`anishift7.pdf`) | 18 | 18 / 18, każda odpowiedź „ANISHIFT7” |
| Audio (`tone.wav`) | 6 (Gemini) | 6 / 6 |
| Wideo (`blue.mp4`) | 6 (Gemini) | 6 / 6, każda odpowiedź „niebieski” |
| Warianty myślenia | 102 | 102 / 102 |
| `probe_palantir_model` | 4 rodziny | 4 / 4 |
| Razem | 182 | **182 / 182** |

`gpt-6-sol` i `gpt-6-luna` (dopisane 2026-09-24, Rozstrzygnięcie 17): osobna próba — warianty `none`, `low`, `medium`, `high`, `xhigh`, `max` i obraz przechodzą; PDF proxy odrzuca (HTTP 400), dlatego ich typy plików to `{image}`.

### Checkpointy

- Przegląd kodu: sześć rund trzech niezależnych recenzentów — w rundach 1–4 świeża `astra` i dwa świeże Claude Opus 5.5, w rundach 5–6 trzy świeże sesje `astra`; runda 6 bez uwag.
- Konto 2: niesprawdzone na żywo — brak tokena 2; mechanizm pokrywają testy przez `LlmService`.
- Tłumaczenie odcinka przez właściciela: oczekuje.
