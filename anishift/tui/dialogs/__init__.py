"""The shared modal primitives every AniShift domain decides through.

Nothing is re-exported here on purpose: a surface imports the one primitive it
needs, so no domain can grow its own frame, its own selector or its own editor.
"""
